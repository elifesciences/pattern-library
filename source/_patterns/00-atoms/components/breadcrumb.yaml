$schema: http://json-schema.org/draft-04/schema#
type: object
properties:
  items:
    type: array
    minItems: 1
    items:
      type: object
      properties:
        term:
          type: string
      required:
        - term
required:
  - items
