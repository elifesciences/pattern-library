$schema: http://json-schema.org/draft-04/schema#
type: object
properties:
  breadcrumb:
    type: string
    minLength: 1
  variant:
    type: string
    enum:
      - article-section
      - asset
  isTruncated:
    type: boolean
    default:
      - false
required:
  - breadcrumb
