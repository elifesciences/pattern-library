{
  "scriptLoader": "{% for javascript in javascripts %}var $script{{loop.index}} = document.createElement('script');$script{{loop.index}}.src = '{{ asset(javascript) }}';$body.appendChild($script{{loop.index}});{% endfor %}"
}
