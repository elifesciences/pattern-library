assets:
  css:
    - image-link.css
    - $ref: picture.yaml#/assets/css
  js: []
schema:
  $schema: http://json-schema.org/draft-04/schema#
  type: object
  properties:
    url:
      type: string
      minLength: 1
    image:
      $ref: picture.yaml#/schema
    variant:
      type: string
      enum:
        - logo
  required:
    - url
    - image
