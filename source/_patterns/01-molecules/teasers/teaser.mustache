<div class="teaser{{#rootClasses}} {{rootClasses}}{{/rootClasses}}">

  {{! These are usually links to major subject areas, see teaser-main-article, but could be unlinked
      text expressing a relationship, e.g. 'Built upon by' in teaser-secondary-related-item}}
  {{#contextLabel}}
    <ol class="teaser__context_label_list">
      {{#list}}
        <li class="teaser__context_label_list_item">
          {{#url}}
            <a href="{{url}}" {{#carouselItem}}tabindex="-1"{{/carouselItem}} class="teaser__context_label_link"><span class="teaser__context_label">{{{name}}}</span></a>
          {{/url}}

          {{^url}}
            <span class="teaser__context_label">{{{name}}}</span>
          {{/url}}
        </li>
      {{/list}}
    </ol>
  {{/contextLabel}}

  <header class="teaser__header">
    {{! For schema: url is dependent on image }}
    {{! Image is either:
          - teaser__img--prominent: full width, above non-article teasers
          - teaser__img--big: full width when viewport narrow, maxing out at 250px wide and floating right as viewport widens
          - teaser__img--small: 70px square image, floating right.
        }}
    {{#image}}
      <a href="{{{url}}}" {{#carouselItem}}tabindex="-1"{{/carouselItem}}><img src="{{defaultPath}}" alt="{{altText}}" {{#srcset}}srcset="{{srcset}}"{{/srcset}} class="teaser__img{{#classes}} {{classes}}{{/classes}}" /></a>
    {{/image}}

    {{! Used for the date in an event teaser only. Treatment very like teaser__img--small.
        Different enough from the date in the meta section, that it gets its own block. }}
    {{#eventDate}}
      {{! For schema: url is dependent on date }}
      {{#url}}
        <a href="{{eventDate.url}}">{{>atoms-date}}</a>
      {{/url}}
      {{^url}}
        <div>{{>atoms-date}}</div>
      {{/url}}
    {{/eventDate}}

    <h4 class="teaser__header_text">
      {{! Occasionally the title isn't linked, for example in the chapter listing item teaser. }}
      {{! For schema: url is dependent on title }}
      {{#url}}
        <a href="{{{url}}}" {{#carouselItem}}tabindex="-1"{{/carouselItem}} class="teaser__header_text_link">{{{title}}}</a>
      {{/url}}
      {{^url}}
        {{{title}}}
      {{/url}}
    </h4>

    {{! secondaryInfo may be a source, an author, something else. Not anticipated to be a list,
        because authors are represented as first author et al.}}
    {{#secondaryInfo}}
    <div class="teaser__secondary_info">
      {{{secondaryInfo}}}
    </div>
    {{/secondaryInfo}}

  </header>

  {{! content could be the impact statement, for example.}}
  {{#content}}
  <div class="teaser__body clearfix">
    {{{content}}}
  </div>
  {{/content}}

  {{! Several things may appear in the footer:
      - meta: standard pattern for article type, or arbitrary text with or without a date
      - publishState: icons representing poa pdf only, or full html in addition is available. Used for research content
      - download icon/link: used to download, found in grid style podcast
   }}
  {{#footer}}
  <footer class="teaser__footer">

    {{#meta}}
      {{> molecules-meta}}
    {{/meta}}

    {{#publishState}}
    <div class="teaser__publish_state">

      {{^vor}}
        <img class="teaser__publish_state--poa" src="{{assetsPath}}/img/icons/poa-1x.png" srcset="{{assetsPath}}/img/icons/poa-2x.png 40w,{{assetsPath}}/img/icons/poa-1x.png 20w" alt="Available as authors' accepted manuscript" />
      {{/vor}}

      {{#vor}}
        <img class="teaser__publish_state--vor" src="{{assetsPath}}/img/icons/vor-1x.png" srcset="{{assetsPath}}/img/icons/vor-2x.png 40w, {{assetsPath}}/img/icons/vor-1x.png 20w" alt="Available as full, typeset article" />
      {{/vor}}

    </div>
    {{/publishState}}

  </footer>
  {{/footer}}
</div>
