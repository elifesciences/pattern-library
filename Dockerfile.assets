ARG image_tag=latest
FROM elifesciences/pattern-library_assets-builder:${image_tag} AS assets-builder
FROM tianon/true:latest

WORKDIR /srv/pattern-library

COPY source/ source/
COPY --from=assets-builder /source/assets/ source/assets/
COPY assets/preload.json source/assets/

ARG description=
LABEL description=${description}
